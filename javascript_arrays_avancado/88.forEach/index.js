// forEach

// forEach só esta disponível dentro de arrays
// forEach só vai iterar sobre os valores
// Recebe uma função

const numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];
numeros.forEach(valor => console.log(valor + 1)); // iterando sobre array